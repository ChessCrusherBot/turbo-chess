# Turbo Chess

Turbo Chess is a Flutter Android chess training app with 30,000 offline drill positions and Stockfish engine play.

## Overview

Turbo Chess is built for focused chess practice on Android. It includes bundled Opening, Middlegame, and Endgame training positions, a Play vs Computer mode powered by Stockfish, game replay, bookmarks, local history, and local progress tracking.

The app is currently free, ad-free, offline-focused, and local-only.

Public source repository: https://github.com/ChessCrusherBot/turbo-chess

## Features

- 30,000 bundled chess drill positions
- Opening, Middlegame, and Endgame training
- Play vs Computer using Stockfish
- Game Replay for completed games
- Bookmarks, history, settings, and progress stored locally on the device
- Android-first Flutter/Dart codebase

## Offline and privacy behavior

Turbo Chess v1 does not use login accounts, analytics, crash reporting, cloud sync, subscriptions, in-app purchases, Google Play Billing, or active AdMob.

The Android release app does not request the `INTERNET` permission. Training content, progress, bookmarks, settings, and game history are local to the device.

## License and third-party notices

Turbo Chess source code is released under the GNU General Public License version 3. The full license text is available in [LICENSE](LICENSE).

Third-party notices are documented in [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md). The Android app also bundles legal text assets under `assets/legal/` so users can view practical license and source information from the installed app.

## Stockfish

Turbo Chess includes Stockfish, licensed under GPLv3.

Bundled Android Stockfish binaries are stored in:

- `android/app/src/main/jniLibs/arm64-v8a/libstockfish.so`
- `android/app/src/main/jniLibs/armeabi-v7a/libstockfish.so`
- `android/app/src/main/jniLibs/x86_64/libstockfish.so`

Recorded Stockfish source and build information is available in:

- `assets/stockfish/STOCKFISH_SOURCE.txt`
- `lib/core/engine/BUILDING_STOCKFISH.md`
- `assets/legal/STOCKFISH_SOURCE.txt`
- `assets/legal/STOCKFISH_BUILDING.md`

## Security and private files

This public repository intentionally does not include Android signing keys, keystore files, local machine configuration, Play Console credentials, environment files, private API keys, or private developer account records.

Release signing and local configuration files are excluded from version control. See [docs/SECRETS_AND_SIGNING.md](docs/SECRETS_AND_SIGNING.md) for signing and secret-handling notes.

## Building from source

Install Flutter and the Android toolchain, then run:

```powershell
flutter pub get
flutter analyze
flutter test
flutter build apk --release
flutter build appbundle --release
```

Release signing depends on a developer's own local signing setup. Local files such as `android/local.properties` are generated by Flutter or Android Studio and are not part of the public repository.
