const rewardedPremiumPassDuration = Duration(days: 3);

const rewardedPremiumPassTitle = '3-Day Premium Pass';

const rewardedPremiumPassDescription =
    'Unlock all positions and remove ads for 3 days.';

const rewardedPremiumPassActivatedMessage =
    'Premium Pass activated for 3 days.';
